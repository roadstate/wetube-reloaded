extends base
include mixins/video 

block content 
    div.profilebg
        img(src="/"+user.avatarUrl).avatar
        h3=user.name
    div.homecontent
        each x in user.videos  
                +video(x)
        else 
            li 업로드한 동영상이 없습니다.